# Retract Radar

A basic project scaffold with a static front end (`index.html`, `app.js`, `style.css`) and a `scripts/` folder for Python utilities.

## Structure

- `index.html` – entry HTML page
- `app.js` – client-side JavaScript
- `style.css` – styles
- `scripts/` – Python scripts
